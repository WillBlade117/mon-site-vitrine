---
import FAQItem from './react/FAQItem.jsx';

const faqs = [
    { 
        question: "Combien de temps faut-il pour créer un site vitrine ?", 
        answer: "Typiquement, un site vitrine est livré en 2 à 4 semaines, après validation du design et du contenu. Le facteur clé est votre réactivité pour fournir les éléments."
    },
    { 
        question: "Mon site sera-t-il optimisé pour le mobile (Responsive) ?", 
        answer: "Absolument. Tous les sites que je développe sont conçus avec une approche mobile-first, garantissant une expérience parfaite sur tous les appareils."
    },
    { 
        question: "Comment mon site va-t-il m'aider à augmenter mes ventes ?", 
        answer: "Grâce à une structure axée sur la conversion, un appel à l'action clair et une optimisation SEO dès la conception, votre site agira comme un commercial 24/7."
    }
];
---

<section id="faq" class="py-24 bg-brand-dark relative overflow-hidden">
    <div class="absolute top-1/2 left-0 -translate-y-1/2 w-64 h-64 bg-brand-primary/5 blur-[100px] rounded-full pointer-events-none"></div>

    <div class="max-w-3xl mx-auto px-6 lg:px-8 relative z-10">
        <h2 class="text-3xl sm:text-5xl font-black text-center text-white mb-16 tracking-tighter">
            Réponses à vos <span class="text-brand-primary font-black">Interrogations</span>
        </h2>
        
        <div class="space-y-2 border-t border-white/10">
            {faqs.map((faq) => (
                <FAQItem 
                    client:load
                    question={faq.question}
                    answer={faq.answer}
                />
            ))}
        </div>

        <div class="mt-20 text-center p-8 rounded-2xl bg-white/[0.02] border border-white/5 backdrop-blur-sm">
            <p class="text-slate-400 mb-6 font-light">D'autres questions en tête ?</p>
            <a href="#contact" class="text-white font-bold hover:text-brand-primary transition-colors flex items-center justify-center gap-2 group">
                Parlons-en de vive voix 
                <span class="group-hover:translate-x-1 transition-transform">→</span>
            </a>
        </div>
    </div>
</section>